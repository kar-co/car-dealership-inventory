# Coding Standards

Use typed Python, Pydantic request/response schemas, SQLAlchemy models, dependency-injected database sessions, and explicit HTTP status codes. Format with Black, sort imports with isort, and lint with flake8.

Use focused React components, semantic HTML controls, Tailwind utility classes, API-derived state, accessible labels, and clear loading/error states. Avoid adding pages or features outside the assignment.
