# Project Overview

The system manages dealership vehicle stock through a FastAPI backend and React frontend. It supports account registration, JWT login, vehicle browsing/search/filtering, authenticated purchase operations, and administrative inventory management.

The assignment deliberately excludes analytics, charts, notifications, extra pages, and public role-management workflows.
