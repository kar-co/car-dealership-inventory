# Git Workflow

Keep changes scoped to the assignment, review the working tree before committing, and include model and Alembic revision changes together. Run formatting and lint checks before opening a review.

For frontend changes, include `package.json` and the lockfile generated by `npm install`, and confirm `npm run build` before review.
