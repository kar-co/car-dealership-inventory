# Project Architecture

The repository contains a FastAPI API, a Django administrative project, a React/Vite frontend, and documentation. The API uses PostgreSQL through SQLAlchemy; Django is not part of API request handling. Vite proxies browser API calls to FastAPI during development.
